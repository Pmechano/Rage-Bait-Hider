# 项目协作约定

本文件适用于整个仓库。修改前阅读本文件及 `README.md`；用户在当前任务中的明确要求优先于这里记录的默认约定。

## 沟通与工作目录

- 默认使用中文沟通，直接说明改动、验证结果及实际限制。
- 提到 WSL 文件路径时，紧跟对应的 Windows 路径。
- 本地项目目录：`/mnt/d/_Factory/RageBaitHider`（Windows：`D:\_Factory\RageBaitHider`）。
- 开始修改前检查 `git status`，保留用户已有改动；不要覆盖、清理或回滚无关文件。
- 用户只询问实现原理时，先解释现有实现，不把猜测说成已经确认的故障原因。

## 项目结构

- `rage-bait-hider.user.js`：完整 Tampermonkey 脚本，直接安装，无构建步骤、无远程运行时依赖。
- `tests/rbh.test.cjs`：Node.js 纯逻辑测试及 Playwright 浏览器模拟测试，使用假 Key 和模拟接口。
- `tests/verify-api.py`：真实 Jev 接口冒烟测试，会读取本地 Key 并消耗额度，不属于默认回归测试。
- `README.md`：安装、更新、功能与兼容性说明，行为改变时同步维护。
- `jevapi.txt`：仅存在于本地的私有 Key 文件，不属于仓库内容。

保持单文件油猴脚本的安装方式。不要为了小改动引入框架、构建工具或公共代理。

## 必须保留的产品行为

### 过滤策略与 Jev

- 主要支持 B 站普通视频页的评论、楼中回复和普通弹幕；用户偏好严格过滤，宁可误杀也不遗漏。
- 默认屏蔽阈值为 `0.30`，概率大于等于阈值时隐藏；未知、待判断、缺失或无效结果均不得自动放行。
- 每条内容只进行一次是否应屏蔽的二元判断。批量请求中的多个 `item_*` 对应不同内容，不要改回多个分类问题。
- 当前模型为 `jev-latest`，使用 TypeSafe `systemone` 接口及 `noul` 输出；读取经过校验的数值，不自行编造分数。
- `[星星眼]`、`[呲牙]`、`[喜极而泣]`、`[打call]` 任一命中即本地屏蔽，对评论和弹幕均生效，在查询模型或复用模型缓存之前执行。
- 表情规则识别文字以及图片的文字说明；不得将规则屏蔽伪装成 Jev 返回的 `1.0`。
- 保留批处理、并发限制、去重、缓存和有限重试，避免增加不必要的付费请求。
- 阈值变化复用已有概率；屏蔽标准或内容上下文变化必须正确隔离缓存。

### 评论及回复

- 同时维护旧式 DOM 和新版多层 Shadow DOM，覆盖后续加载、节点复用、正文变化和视频切换。
- 先判断主评论，只有主评论通过才提交楼中回复。主评论待判断、被屏蔽、判断失败或无法识别时，其回复不得请求 Jev。
- 隐藏整组评论时，包含所有楼中回复以及“共 x 条回复，点击查看”等展开入口；仅隐藏主评论正文不满足要求。
- 回复在排队和重试发送前重新检查主评论状态。不再满足条件时取消尚未发送的任务；主评论重新通过后能够恢复判断。
- 已发送的请求无法撤回。过期响应不得放行已复用的节点、另一视频的内容或被屏蔽主评论下的回复。
- 临时显示全部评论只能解除显示遮挡，不能绕过主评论通过后才提交回复的规则。
- 只处理页面已加载内容，不默认自动展开回复或爬取全部历史评论。

### Debug 显示

- Debug 默认关闭，切换即时生效并保存设置，不额外请求 Jev。
- 将分数追加在评论和回复的正文末尾，使用灰色字体；不要恢复为依赖昵称定位。
- 新版评论优先定位 `bili-rich-text` 内的正文。注意 `querySelector` 的逗号分组按 DOM 顺序匹配，不代表选择器优先级；需要优先级时分别查询。
- 标签显示真实模型概率或缓存值；未调用、待判断、失败应如实标明。
- `data-rbh-debug` 标签必须从正文和父评论提取中排除，标签自身的增删和文字变化不能造成重复判断或观察器循环。
- 页面重绘后恢复标签，避免重复插入；关闭 Debug 时移除标签。

### 弹幕

- 保留 B 站原生 DOM、轨道、动画、时序及显示设置，只遮挡未通过筛选的内容。
- 不恢复自绘弹幕层，不强制显示播放器本身关闭或屏蔽的内容。
- 通过 `web/view` 读取分段数量，经 `web/seg.so` 预取；预取与播放时出现的相同文字共用判断。
- 预取失败时仍能判断原生 DOM 中出现的文字；模型请求失败不应放行未判断内容。
- 已知容器中的 Canvas、特殊弹幕层及无法识别的内容保持隐藏；不要宣称已经支持尚未适配的渲染方式。
- 不监听每帧动画样式变化来触发全量扫描，避免高频无效工作。

## 密钥与数据

- 不把 API Key 写入脚本、文档、测试、日志、提交信息或命令行参数；不要输出 `jevapi.txt` 内容。
- 保留 `.gitignore` 对 `jevapi.txt`、`*.private.user.js`、`node_modules/`、`test-results/` 的排除。
- 提交使用明确文件列表。发布前确认私有文件未被跟踪，待发布文件及提交历史不含真实 Key；检查只输出通过与否，不输出密钥。
- 日常回归使用模拟接口。仅在任务确有需要且已有相应授权时运行真实付费测试，不要为了 UI 或文档修改调用真实模型。
- 模型只接收判断所需的标题、目标正文、类型及可取得的父评论；不得把 B 站 Cookie 或无关账号信息发给模型服务。

## 验证

脚本改动至少运行语法和相应逻辑检查：

```bash
node --check rage-bait-hider.user.js
node tests/rbh.test.cjs
git diff --check
```

涉及 DOM、Shadow DOM、请求队列、显示状态或 Debug 时，使用已安装的 Playwright 和 Chromium 运行浏览器测试：

```bash
RBH_BROWSER_TESTS=1 node tests/rbh.test.cjs
```

非标准安装位置可设置 `RBH_PLAYWRIGHT_PATH` 和 `PLAYWRIGHT_BROWSERS_PATH`。不要假设上一次会话的临时依赖目录仍存在。浏览器执行被沙箱限制时按环境提供的权限机制处理，不把环境启动失败当成功能测试通过。

- 未开启 `RBH_BROWSER_TESTS` 时浏览器用例会跳过，报告中必须区分通过和跳过。
- 测试验证用户能观察到的行为，以及实际提交给模型的模拟请求，不只检查元素是否存在。
- Debug 应验证标签实际可见、颜色、位置、重绘恢复和正文不被污染。
- 回复过滤应验证请求顺序、主评论不通过时无回复请求，以及排队取消和恢复。
- 纯文档修改无需运行浏览器或付费 API 测试；检查文档与现有实现一致即可。
- 本地模拟测试不等于真实 B 站验证。此前开发环境访问 B 站接口返回 HTTP 412；真实页面未验证的部分必须如实说明。

## 版本与发布

- GitHub 仓库为 `Pmechano/Rage-Bait-Hider`，主分支为 `main`。沿用当前任务已有的提交和发布授权，不强推或改写历史。
- 发布脚本行为变更时提高 `@version`，同步 README 中的版本和说明。仅修改文档或本文件不需要提高脚本版本。
- 保持 `@name`、`@namespace` 及现有 GM 存储键稳定，避免丢失用户 Key 和设置。
- `@updateURL` 和 `@downloadURL` 都应指向：
  `https://raw.githubusercontent.com/Pmechano/Rage-Bait-Hider/main/rage-bait-hider.user.js`
- 发布前完成适用测试和密钥检查，提交后推送到已配置的远端。推送失败时如实区分“已本地提交”与“已发布”。
- 发布脚本后验证公开原始文件确实提供新版，再告知用户检查更新并刷新视频页。
- 当前本地环境曾通过 Windows Git 的凭据助手完成 HTTPS 推送；WSL SSH 未配置可用身份。需要时使用已有凭据，不读取或输出令牌。
